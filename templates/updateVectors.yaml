adapter: updateVectors
protocol: grpc

request:
  rpc: qdrant.Points/UpdateVectors
  message: |
    {
      "collectionName": "{{collectionName}}",
      "wait": true,
      "points": {{points}}
    }

response:
  extract:
    status: result.status
